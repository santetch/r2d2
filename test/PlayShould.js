import {expect} from "chai";

const {mock} = global;


describe('Play should', () => {

    beforeEach(() => {

    });

    context('in one round', () => {
        it('roll 0 pins', () => {

            expect(1).examples
        });

    });
});